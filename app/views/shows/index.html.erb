<h1>Listing shows</h1>
<h2>Pick from the following list of shows</h2>

<%#= render 'list' %>
<br>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Category</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @shows.each do |show| %>
      <tr>
        <td><%= show.showName %></td>
        
        <td><%= show.category %></td>
        <td><%= show.description %></td>
        <td><%= link_to 'Show', show, class: "content-link" %></td>
        <td><%= link_to 'Edit', edit_show_path(show), class: "content-link"  %></td>
        <td><%= link_to 'Destroy', show, method: :delete, data: { confirm: 'Are you sure?' }, class: "content-link" %></td>
       
      </tr>
    <% end %>

  </tbody>
</table>
<br>

<%= link_to 'New Show', new_show_path, class: "content-link"  %>
